<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
    <div class="container">
        <% if (message) { %>
            <div class="message-box"><%= message %></div>
        <% } %>
        <h2>Bem-vindo, <%= email %>!</h2>
        <p>Usuário logado.</p>
        <a href="/logout">Sair</a>
        <hr>

        <h3>Status da Assinatura</h3>
        <% if (subscriptionStatus === 'active') { %>
            <p style="color: green; font-weight: bold;">Plano Ativo! Você já pode gerenciar suas empresas.</p>
        <% } else if (subscriptionStatus === 'pending') { %>
            <p style="color: orange; font-weight: bold;">Assinatura Pendente. Por favor, finalize o pagamento no Mercado Pago.</p>
        <% } else { %>
            <p>Seu plano está inativo.</p>
            <a href="/subscription">
                <button>Assinar Agora</button>
            </a>
        <% } %>
    </div>
</body>
</html>